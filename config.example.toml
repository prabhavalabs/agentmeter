# Copy this file to the location printed by `agentmeter doctor`.

[general]
# The desktop app stores later changes in a private Application Support overlay.
poll_interval_seconds = 300
providers = ["codex", "claude", "gemini", "cursor"]

[display]
brightness_percent = 55
dim_after_seconds = 300
screen_off_after_seconds = 1800
alert_thresholds = [75, 90]
sound_enabled = false

[transport]
preferred = "ble"
device_name = "AgentMeter"
